# Project Skills

This repository vendors a small set of agent "skills" under `.claude/skills/`.

Notes:
- These were copied from `facebook/react` at commit `da64117876002bd60baa9e93cad77421bbf0293f`.
- The contents are React-specific (commands like `yarn flow`, `yarn test-www`).
- Keep this directory focused on reusable, reviewed skills (not caches or logs).

