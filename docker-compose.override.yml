version: '3.8'

# Development overrides:
# - bind-mount source code for hot reload
# - enable uvicorn reload for backend
services:
  backend:
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    volumes:
      - .:/app

