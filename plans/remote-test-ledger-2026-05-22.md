# Remote Test Ledger - 2026-05-22

Scope: server-side verification on `/data/MimirQ` at `10.168.2.253`.

This ledger records what has already been tested remotely so future runs do not repeat expensive parser, KG, or RAG checks unless the related code/config changes.

## Completed

| Area | Evidence | Result | Notes |
| --- | --- | --- | --- |
| Docker/root env wiring | `plans/server-integration-verification-2026-05-22.md` | pass | API/worker restarted with root `.env`; parser URLs aligned to Docker service names. |
| Parser matrix smoke | `artifacts/parser-matrix/remote-20260522-051241` | pass | `Calls: 411 | Failures: 0 | Missing: 0`. |
| Explicit parser backends | `artifacts/parser-matrix/remote-detail-20260522-052736/report.json` | pass | `basic`, `markitdown`, `docling`, `mineru`, `magicpdf`, `paddle_vl`, `textin` returned the requested backend. |
| Remote API chain | `artifacts/remote-api-chain/remote-api-chain-20260522-055729/report.json` | pass | 4 documents completed; parsed markdown persisted; 5 chunk preview strategies; KG extract/search/stats; RAG retrieve; chat. |
| RAG retrieve/chat | `artifacts/remote-api-chain/remote-api-chain-20260522-055729/report.json` | pass | 4 citations; chat status 200; chat elapsed 0.209s. |
| KG smoke | `artifacts/remote-api-chain/remote-api-chain-20260522-055729/report.json` | pass | Heuristic extraction passed for 3 docs; each document took about 5.8s-6.9s. |
| Chunking matrix extension | `artifacts/chunk-governance-matrix/remote-20260522-063015/report.json` | pass | 15/15 product-relevant strategies passed through `/api/v1/documents/chunk-preview`; max elapsed 0.034s. |
| Governance preview matrix | `artifacts/chunk-governance-matrix/remote-20260522-063015/report.json` | pass | 6/6 governance cases passed through `/api/v1/pipeline/clean-preview`: PII, secrets, URL normalization, duplicate drop, HTML/table normalization, low-density drop. |
| KG scale guard | `artifacts/kg-scale-guard/remote-20260522-072703/report.json` | pass | 12/12 documents completed and 12/12 heuristic KG extracts returned 200 after API/worker images were rebuilt. Avg extract 8.329s, P95 14.868s, max 14.928s; KG stats 200 in 0.153s; KG search 200 in 0.408s. |
| API/worker retry runtime | remote Docker containers on `/data/MimirQ` | pass | Rebuilt API/worker images and verified running worker config: document max tries 80, KG max tries 80, retry defer 30s. This fixed the earlier 12-doc run where 2 docs stayed pending after 3 worker retries. |
| Large PDF parser baseline | `artifacts/pdf-performance/remote-20260522-081140/report.json` | pass | 144-page arXiv PDF (`2303.18223`, 5.85 MB): `basic` 4.011s, `markitdown` 55.557s, `docling` 340.648s. |
| MinerU VLM GPU parser | `artifacts/pdf-performance/remote-20260522-092203/report.json` | pass | Same 144-page PDF through MimirQ API with `MINERU_BACKEND=vlm-http-client`: `mineru` 241.55s, 886,217 markdown chars, page count 144. VLM service returned `/v1/models`; observed A6000 memory about 39.9 GiB and GPU utilization up to 91% during parsing. |
| Non-MinerU parser/OCR service matrix | `artifacts/parser-service-matrix/remote-20260522-122334/report.json`, `artifacts/parser-service-matrix/remote-20260522-123236/report.json` | pass with one expected disabled backend | Live `/api/v1/pipeline/parse-preview` on a generated 2-page PDF: `basic` 1.503s, `deepdoc` 14.212s, `docling` 173.276s, `magicpdf` 159.388s, `markitdown` 2.502s-2.690s, `etl4llm` 1.900s-1.902s, `marker` 16.035s-16.095s, `paddle_vl` 15.245s-16.857s, `olmocr` 275.565s-344.612s, `textin` 2.293s-2.507s, `deepseek_ocr` 3.932s-4.167s all returned 200 with markdown. `qianfan_ocr` returned 400 because `QIANFAN_OCR_ENABLED=false` and `QIANFAN_OCR_API_URL` is empty; wrapper health passed but real OCR is not configured. |
| Qianfan online OCR parser | `artifacts/parser-service-matrix/remote-20260522-135309/report.json` | pass | `qianfan_ocr` is now enabled against Baidu Qianfan online `deepseek-ocr`; live `/api/v1/pipeline/parse-preview` returned status 200 in 4.733s with 854 markdown chars across 19 lines. Wrapper health reported model `deepseek-ocr` and configured upstream. |
| 144-page server parser expansion | `artifacts/pdf-performance/remote-20260522-142307/report.json` | partial | Same 144-page arXiv PDF: `deepdoc` truly resolved to `deepdoc` in 1103.709s with 737,401 markdown chars; `etl4llm` resolved to `etl4llm` in 14.594s with 862,340 chars and 20 images. `magicpdf`, `marker`, `paddle_vl`, and `olmocr` returned HTTP 200 but resolved to `basic`, so they are fallback outputs rather than successful advanced-parser runs. |
| 144-page direct serial parser verification | `artifacts/direct-parser-serial-144/20260523-011021/summary.json`, `artifacts/direct-parser-fixups-144/20260523-021914/` | mixed | Clean serial runs with `mineru-vlm` stopped for non-MinerU tests: `paddle_vl` failed after 2965.148s with HTTP 500 from its internal `doc_parser`; `olmocr` succeeded in 494.997s with 889,603 markdown chars; `marker` no longer had a connection bug once service readiness was handled, but the 144-page request still timed out client-side after 7100.124s; `magicpdf` direct invocation in the API container failed immediately because the correct runtime needs `/opt/venv/bin/python`, and even there the backend is not runnable because `magic-pdf` CLI is absent from `PATH`. |
| MagicPDF serviceization remote validation | `artifacts/parser-service-matrix/magicpdf-service-final-20260523-000212/report.json`, `artifacts/pdf-performance/magicpdf-service-20260523-000543/report.json` on `10.168.2.253:/tmp/MimirQ-main` | pass | Rebuilt `mimirq-magicpdf` from `main@77e52ef7`, switched API/worker to service mode via root `.env`, and verified `settings/status` reports `configured (service)`. 2-page parse-preview resolved `backend=magicpdf` in 19.678s. The 144-page arXiv PDF (`2303.18223`, 5.85 MB) also resolved `backend=magicpdf` in 163.729s with 810,438 markdown chars and page count 144. |

## Next Remote Tests

| Priority | Area | Why |
| --- | --- | --- |
| P0 | Product quality backlog | Track the next pass under `plans/server-integration-verification-2026-05-22.md`: parsing serviceization, KG density, chunking on real parsed outputs, prompt quality, knowledge-base lifecycle, governance, and admin/permission boundaries. |
| P2 | Parser batch/concurrency performance | Single large PDF latency is measured; still need controlled batch/concurrency runs before changing production defaults. |
| P2 | Browser UI walkthrough | API is verified; UI click-through has not been rerun for this server deployment. |

## Known Gaps

- Broader product verification is now tracked in `plans/server-integration-verification-2026-05-22.md`, covering parsing, KG, chunking, prompts, knowledge-base lifecycle, governance, and admin/permission behavior. Remote test artifacts should link back to that task instead of becoming disconnected one-off runs.
- `scripts/production_readiness_chain.py` is not portable on the remote host/container as-is: host lacks `python-docx`, API container lacks `requests`.
- Use `scripts/remote_api_chain_smoke.py` for dependency-light server checks until the full readiness script is made portable.
- KG scale is functionally passing for 12 synthetic documents. Next optimization is extraction density/latency tuning on larger real PDFs, not another small-doc smoke rerun.
- KG event density and usefulness need product discussion before changing defaults. The current 12-doc scale guard generated 12 events because each synthetic document contains one obvious incident; this proves the channel is bounded and stable, but not that the event budget is useful for real KG-assisted RAG. Later discussion should compare baseline RAG vs KG-assisted RAG on real questions before increasing extraction density.
- Qianfan OCR is passing with the online Baidu Qianfan `deepseek-ocr` path on the remote server. Keep `QIANFAN_OCR_PROMPT` empty for that endpoint unless retested; the wrapper selects a short OCR prompt because longer Markdown conversion prompts returned empty output in live probes.
- OLMOCR, Docling, and MagicPDF are functional but slow on the small 2-page smoke PDF. On the 144-page PDF, HTTP 200 alone is not enough: direct serial runs showed `olmocr` can complete in about 495s on a clean GPU, `paddle_vl` still fails after about 49 minutes, and `marker` still times out after about 7100s even when the earlier service-readiness bug is removed. `scripts/remote_pdf_parser_performance.py` and `scripts/remote_parser_service_matrix.py` now treat requested/resolved backend mismatch as a failure class.
- Follow-up on real parsed outputs: the same 144-page MagicPDF document (`f20f6207-f808-4bd9-a8ae-8c2d92455793`) persisted `670` chunks and about `816k` chars after ingestion, but chunk-preview strategy spread is very large on the raw PDF path (`langchain_recursive=1151`, `parent_child=3702`, `semantic_sentence=1272`, `markdown_hierarchy=6203`). This needs product selection rather than assuming one default fits all long PDFs.
- Follow-up on real KG density: a default LLM KG extraction probe on dataset `cd232ae7-609d-415f-a43a-cca768aee664` was still running after `250+` extraction batches and had not committed final graph stats inside the probe window. This is enough evidence that long-document KG extraction needs a tighter bounded policy before it can be treated as a default production path.
- The API container's default `python` is not the app virtualenv. For direct parser debugging inside `docker-mimirq-api-1`, use `/opt/venv/bin/python`; otherwise imports such as `langchain_core` will appear missing even though they exist in the app venv.
- Direct `magicpdf` inside the API container remains intentionally non-production and still should not be used as the deployment path. The validated production path is the standalone `mimirq-magicpdf` service image, which now also patches MagicPDF's internal `lang.ch` OCR model mapping to the shared `ch_PP-OCRv5_rec_infer.pth` cache layout when the older doc-recognition checkpoint is absent.
