# Remote Test Ledger - 2026-05-22

Scope: server-side verification on `/data/MimirQ` at `10.168.2.253`.

This ledger records what has already been tested remotely so future runs do not repeat expensive parser, KG, or RAG checks unless the related code/config changes.

## Completed

| Area | Evidence | Result | Notes |
| --- | --- | --- | --- |
| Docker/root env wiring | `plans/server-integration-verification-2026-05-22.md` | pass | API/worker restarted with root `.env`; parser URLs aligned to Docker service names. |
| Parser matrix smoke | `artifacts/parser-matrix/remote-20260522-051241` | pass | `Calls: 411 | Failures: 0 | Missing: 0`. |
| Explicit parser backends | `artifacts/parser-matrix/remote-detail-20260522-052736/report.json` | pass | `basic`, `markitdown`, `docling`, `mineru`, `magicpdf`, `paddle_vl`, `textin` returned the requested backend. |
| Remote API chain | `artifacts/remote-api-chain/remote-api-chain-20260522-055729/report.json` | pass | 4 documents completed; parsed markdown persisted; 5 chunk preview strategies; KG extract/search/stats; RAG retrieve; chat. |
| RAG retrieve/chat | `artifacts/remote-api-chain/remote-api-chain-20260522-055729/report.json` | pass | 4 citations; chat status 200; chat elapsed 0.209s. |
| KG smoke | `artifacts/remote-api-chain/remote-api-chain-20260522-055729/report.json` | pass | Heuristic extraction passed for 3 docs; each document took about 5.8s-6.9s. |
| Chunking matrix extension | `artifacts/chunk-governance-matrix/remote-20260522-063015/report.json` | pass | 15/15 product-relevant strategies passed through `/api/v1/documents/chunk-preview`; max elapsed 0.034s. |
| Governance preview matrix | `artifacts/chunk-governance-matrix/remote-20260522-063015/report.json` | pass | 6/6 governance cases passed through `/api/v1/pipeline/clean-preview`: PII, secrets, URL normalization, duplicate drop, HTML/table normalization, low-density drop. |
| KG scale guard | `artifacts/kg-scale-guard/remote-20260522-072703/report.json` | pass | 12/12 documents completed and 12/12 heuristic KG extracts returned 200 after API/worker images were rebuilt. Avg extract 8.329s, P95 14.868s, max 14.928s; KG stats 200 in 0.153s; KG search 200 in 0.408s. |
| API/worker retry runtime | remote Docker containers on `/data/MimirQ` | pass | Rebuilt API/worker images and verified running worker config: document max tries 80, KG max tries 80, retry defer 30s. This fixed the earlier 12-doc run where 2 docs stayed pending after 3 worker retries. |
| Large PDF parser baseline | `artifacts/pdf-performance/remote-20260522-081140/report.json` | pass | 144-page arXiv PDF (`2303.18223`, 5.85 MB): `basic` 4.011s, `markitdown` 55.557s, `docling` 340.648s. |
| MinerU VLM GPU parser | `artifacts/pdf-performance/remote-20260522-092203/report.json` | pass | Same 144-page PDF through MimirQ API with `MINERU_BACKEND=vlm-http-client`: `mineru` 241.55s, 886,217 markdown chars, page count 144. VLM service returned `/v1/models`; observed A6000 memory about 39.9 GiB and GPU utilization up to 91% during parsing. |
| Non-MinerU parser/OCR service matrix | `artifacts/parser-service-matrix/remote-20260522-122334/report.json`, `artifacts/parser-service-matrix/remote-20260522-123236/report.json` | pass with one expected disabled backend | Live `/api/v1/pipeline/parse-preview` on a generated 2-page PDF: `basic` 1.503s, `deepdoc` 14.212s, `docling` 173.276s, `magicpdf` 159.388s, `markitdown` 2.502s-2.690s, `etl4llm` 1.900s-1.902s, `marker` 16.035s-16.095s, `paddle_vl` 15.245s-16.857s, `olmocr` 275.565s-344.612s, `textin` 2.293s-2.507s, `deepseek_ocr` 3.932s-4.167s all returned 200 with markdown. `qianfan_ocr` returned 400 because `QIANFAN_OCR_ENABLED=false` and `QIANFAN_OCR_API_URL` is empty; wrapper health passed but real OCR is not configured. |
| Qianfan online OCR parser | `artifacts/parser-service-matrix/remote-20260522-135309/report.json` | pass | `qianfan_ocr` is now enabled against Baidu Qianfan online `deepseek-ocr`; live `/api/v1/pipeline/parse-preview` returned status 200 in 4.733s with 854 markdown chars across 19 lines. Wrapper health reported model `deepseek-ocr` and configured upstream. |
| 144-page server parser expansion | `artifacts/pdf-performance/remote-20260522-142307/report.json` | partial | Same 144-page arXiv PDF: `deepdoc` truly resolved to `deepdoc` in 1103.709s with 737,401 markdown chars; `etl4llm` resolved to `etl4llm` in 14.594s with 862,340 chars and 20 images. `magicpdf`, `marker`, `paddle_vl`, and `olmocr` returned HTTP 200 but resolved to `basic`, so they are fallback outputs rather than successful advanced-parser runs. |
| 144-page direct serial parser verification | `artifacts/direct-parser-serial-144/20260523-011021/summary.json`, `artifacts/direct-parser-fixups-144/20260523-021914/` | mixed | Clean serial runs with `mineru-vlm` stopped for non-MinerU tests: `paddle_vl` failed after 2965.148s with HTTP 500 from its internal `doc_parser`; `olmocr` succeeded in 494.997s with 889,603 markdown chars; `marker` no longer had a connection bug once service readiness was handled, but the 144-page request still timed out client-side after 7100.124s; `magicpdf` direct invocation in the API container failed immediately because the correct runtime needs `/opt/venv/bin/python`, and even there the backend is not runnable because `magic-pdf` CLI is absent from `PATH`. |
| MagicPDF serviceization remote validation | `artifacts/parser-service-matrix/magicpdf-service-final-20260523-000212/report.json`, `artifacts/pdf-performance/magicpdf-service-20260523-000543/report.json` on `10.168.2.253:/tmp/MimirQ-main` | pass | Rebuilt `mimirq-magicpdf` from `main@77e52ef7`, switched API/worker to service mode via root `.env`, and verified `settings/status` reports `configured (service)`. 2-page parse-preview resolved `backend=magicpdf` in 19.678s. The 144-page arXiv PDF (`2303.18223`, 5.85 MB) also resolved `backend=magicpdf` in 163.729s with 810,438 markdown chars and page count 144. |
| Full real-PDF API chain | `artifacts/real-pdf-chain/final-20260523-014839/report.json` on `10.168.2.253:/tmp/MimirQ-main` | pass with one follow-up | End-to-end live API chain on the 144-page arXiv PDF: upload -> document completion -> chunks -> chunk previews -> default KG extract -> baseline chat -> graph chat. Document `d7359385-2767-4c09-b18f-c7c4a9ccbb86` completed with `670` stored chunks; default KG extract completed in `13.616s` with `120 events / 1148 entities / 1887 links`. Baseline chat took `8.09s`; graph-enabled chat took `0.217s`. |
| Real-PDF lifecycle cleanup | manual API verification on dataset `f833c04d-e64e-46ac-8773-bc77a81d6ab9` at `10.168.2.253:/tmp/MimirQ-main` | pass | `POST /api/v1/datasets/{id}/purge?dry_run=false` deleted `1` document in `1.646s`; `GET /api/v1/datasets/{id}/documents/export` then returned `0` documents; `GET /api/v1/kg/stats` returned zero graph assets; `DELETE /api/v1/datasets/{id}` returned `204`. |
| Real-PDF chain with scripted cleanup | `artifacts/real-pdf-chain/cleanup-20260523-022146/report.json` on `10.168.2.253:/tmp/MimirQ-main` | pass | The same 144-page PDF completed full chain + cleanup through `remote_real_pdf_chain.py --cleanup-mode purge_dataset --delete-dataset-after`. Document `3f071750-27ca-47e8-8e8f-1250bdf03b96` completed with `670` stored chunks; default KG extract took `13.49s`; purge deleted `1` document in `1.327s`; post-cleanup dataset export returned `0` documents; post-cleanup KG stats returned zero graph assets; dataset delete returned `204`. |
| Permission matrix smoke + access graph | `artifacts/permission-matrix/access-graph-20260523-025503/report.json` on `10.168.2.253:/tmp/MimirQ-main-accessgraph` | pass | Admin allow verified for `settings/status`, `groups`, `access-graph/summary`, `access-graph/export`, and document ACL management. Outsider received `403` on the same admin-only endpoints plus document access `GET/PUT`. Cleanup of the disposable dataset also succeeded; the script still carries role-normalization logic for this host's dev bootstrap behavior. |
| Governance ingest matrix | `artifacts/governance-ingest-matrix/remote-20260523-031333/report.json` on `10.168.2.253:/tmp/MimirQ-main-governance-pass` | pass | Live ingest proof across five governance cases: PII masking, secrets masking, HTML rule-pack cleanup (`web_navigation`, `web_cookie_banners`) with table normalization, duplicate paragraph cleanup, and outline-only quarantine. Four completed cases each retained `1` chunk and `1` retrieval citation after cleaning; the quarantine case produced `status=quarantined` with `governance_drop_reasons.outline_only=1`. Cleanup purged `5` documents, reset KG stats to zero, and deleted the dataset. |
| Dataset precheck batch probe | `artifacts/precheck-batch/remote-20260524/report.json` from `scripts/remote_precheck_batch_probe.py` against `10.168.2.253` | pass | A dedicated precheck-batch probe now proves the `upload-batch + precheck_only` path works end-to-end on the live API. The run created a disposable dataset, uploaded one Markdown file through `/api/v1/documents/upload-batch` with `precheck_only=true`, captured `precheck_scan_run_id=1c1a8bc4-ee49-4f88-84e5-2c2ca1feb1d1`, and waited for the resulting dataset precheck scan run to reach `completed`. The follow-up summary payload reported `total_files=1`, `by_file_type.md=1`, and a `short_text` finding, while the samples payload returned one representative sample with `text_characters=66` and `findings=['short_text']`. Cleanup deleted the disposable dataset (`204`). |
| Prompt workflow matrix | `artifacts/prompt-matrix/remote-20260523-034943/report.json` on `10.168.2.253:/tmp/MimirQ-main-prompt-reg` | pass | Builtin prompt sync updated all 4 operational templates. `rag_answer_claude_xml_zh` was proven on `prompt-preview` and live `/chat` (`2` citations, correct blue-flag answer). `kg_extract_graphrag_zh` was proven via KG event `extra_data.kg_prompt_template_key`. `testset_generation_ragas_zh` generated `2` document questions and saved `2` regression cases. `judge_faithfulness_ragas_zh` was proven via regression run `326a5f12-c49b-4f05-8cdd-ca669afe0c7b`, whose summary and item metadata both recorded the expected judge prompt key. Cleanup deleted the generated regression cases, purged `2` documents, reset KG stats to zero, and deleted the dataset. |
| KG usefulness matrix | `artifacts/kg-usefulness-matrix/remote-20260523-052501/report.json`, `artifacts/kg-usefulness-matrix/remote-broader-20260523/report.json` on `10.168.2.253` | pass | KG usefulness now has two complementary server proofs on the same 3-document synthetic corpus. The strict targeted proof still covers the two multi-hop questions (`11` clues / `3` events for acquisition->leader, `6` clues / `2` events for leader->service) with diagnostics `baseline_hit_rate=1.0`, `baseline_recall=1.0`, and explicit extractive answers for `Mira Chen` and `Orion billing service`. The broader proof expands coverage to `5` questions by adding `What happened after Project Atlas acquired Blue Harbor?`, `Summarize this corpus in one sentence.`, and `What is the overall story across these documents?`. In that broader run, all `5` questions returned non-zero clues (`11`, `6`, `11`, `4`, `3`), both baseline and graph chat passed their citation/expectation gates on every question, and diagnostics still held at `baseline_hit_rate=1.0`, `baseline_recall=1.0`. Cleanup deleted generated regression cases, purged documents, reset KG stats to zero, and deleted the dataset. |
| Production readiness chain portability (remote host) | `artifacts/production-readiness/portable-remote-20260523/report.json` from `scripts/production_readiness_chain.py` on `10.168.2.253:/tmp/MimirQ-main-webfix` | pass | The full production-readiness chain now runs on the remote host even though that host lacks `python-docx` and `openpyxl`. The run used a pre-staged corpus plus the new stdlib/checked-in-sample fallbacks, created dataset `5152bf1d-584e-49b3-9788-448cfa34b928`, uploaded `12` documents across `txt`, `md`, `html`, `csv`, `pdf`, `docx`, and `xlsx`, completed all documents, produced `1370` total chunks, returned `510 events / 2287 entities / 7266 links`, kept KG search and RAG retrieval under `3s`, and passed both chat gates with `failures=[]`. |
| Production readiness chain portability (API container) | `artifacts/production-readiness/container-remote-20260523/report.json` from `scripts/production_readiness_chain.py` inside `docker-mimirq-api-1` on `10.168.2.253` | pass | The same full readiness chain now also runs inside the current API container when invoked with `/opt/venv/bin/python` and a staged corpus. The container run created dataset `65b4172b-6f70-4579-ab50-44c50672d8a8`, uploaded `12` mixed-format documents, completed all documents, produced `1381` chunks, returned `519 events / 2293 entities / 7402 links`, kept KG search and RAG retrieval under `3s`, and passed both chat gates with `failures=[]`. This pairs with the host proof to show the chain is portable across both execution surfaces used in this task. |
| Observability metrics extractive probe | `artifacts/observability-metrics/remote-20260524/report.json` from `scripts/remote_observability_metrics_probe.py` against `10.168.2.253` | pass | A dedicated observability probe now proves that explicit extractive chat responses are visible to metrics dashboards after `main@0f4aedb5`. The script temporarily enables `ENABLE_METRICS_LOG`, creates one disposable dataset and Markdown document, runs one grounded extractive chat plus one keyword-mode zero-hit extractive chat, then polls `/api/v1/observability/rag-metrics/summary` and `/api/v1/observability/rag-metrics/query-analytics` until both counters move. In the captured report, summary `rag_trace_count` advanced from `1` to `3`, query-analytics `rag_trace_count` advanced from `1` to `3`, `zero_hit_count` advanced from `0` to `1`, and the zero-hit hash list now includes the generated query hash. Cleanup deletes both conversations, purges the disposable dataset, deletes the dataset, and restores the original observability settings. |
| KG quality report probe | `artifacts/kg-quality-report/remote-20260524/report.json` from `scripts/remote_kg_quality_report_probe.py` against `10.168.2.253` | pass | A dedicated KG quality-report probe now proves the `/api/v1/evaluations/kg/quality/report` path works end-to-end after the PostgreSQL JSON predicate fix. The script creates one disposable dataset and one Markdown document, waits for completion, runs heuristic KG extraction, then calls the quality-report endpoint and validates the returned aggregate payload. In the captured report, the endpoint returned `200` with `summary.documents=1`, `summary.events=1`, `summary.entities=2`, plus populated `components`, `noise`, `truncation`, and `scope` sections where `scope.documents_allowed=1`. Cleanup purges the disposable document, deletes the dataset, and records each step in the artifact. |
| Browser UI walkthrough (remote backend) | `artifacts/ui-clickthrough/backend-business-surfaces-live-20260523-remote-backend.json` from local Playwright + SSH tunnel to `10.168.2.253` | pass | Live frontend business-surface smoke passed `1/1` in `58.336s`. It proved real visits and interactions for `/settings` (industry-rules preview + RTBF section), `/graph`, `/datasets/{id}/profile`, and `/history?id=...` against the current remote backend. The spec had to be updated to current UI labels (`行业规则`, `预览`). |
| Browser UI walkthrough (remote web page host) | `artifacts/ui-clickthrough/backend-business-surfaces-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated `remote-web` lane passed `1/1` in `12.246s` after rebuilding the remote `web` container from `main@059028d9`. It proved the same `/settings`, `/graph`, `/datasets/{id}/profile`, and `/history?id=...` business-surface interactions against the deployed frontend page host itself, closing the remaining deployment-specific UI caveat. |
| Browser UI live stack workflow (remote web page host) | `artifacts/ui-clickthrough/live-stack-remote-web-20260523.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | A deeper live-stack smoke passed `1/1` in `27.282s` after deploying `main@ca40dd49`. It exercised upload -> parse -> viewer -> real chat -> command-menu handoff on the deployed frontend page host. The run also proved the new cross-encoder cold-start guard works in production: the first `BAAI/bge-reranker-v2-m3` download attempt timed out after `2.000s`, retrieval fell back to base order, assistant replies still completed, and API health stayed green. Cleanup deleted the `enterprise-telemetry-sample.md` parsing documents created by the smoke. |
| Browser UI management surfaces (remote web page host) | `artifacts/ui-clickthrough/management-surfaces-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated management-surface lane now passes `4/4` in `25.519s`. It proves real admin page loads for `/prompts`, `/reports`, `/evaluations`, `/usage`, `/audit`, `/settings/rbac`, `/settings/groups`, one live `/settings/groups/{id}` detail page, and `/access-review -> /audit`, plus three real mutation workflows on the deployed page host: create group + add member, RBAC role change + revert, and prompt-template create + delete. The run hit live backend-admin surfaces such as prompt templates, dataset reports, evaluation runs, usage summaries, audit logs, tenant-member listings, and group CRUD/membership APIs while the API stayed healthy. |
| Browser UI usage workflow (remote web page host) | `artifacts/ui-clickthrough/usage-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated usage lane now passes `1/1` in `3.053s`. The test opens `/usage` on the deployed page host, verifies live top-level usage totals against the current `/api/v1/usage/chat/tokens/summary`, `/usage/chat/cost/summary`, and `/usage/tenant/quotas` payloads, then opens the `租户级配额状态` raw-response section and confirms the rendered JSON includes the expected `documents`, `storage`, `embedding_chars`, and `qps` quota sections. This lifts `/usage` from a shell-only management proof into a panel-level proof focused on the tenant quota surface. |
| Browser UI quarantine review workflow (remote web page host) | `artifacts/ui-clickthrough/quarantine-surfaces-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated quarantine-review lane now passes `2/2` in `13.767s`. It creates real outline-only quarantined documents through the live API and proves two UI review actions on the deployed page host: `标记为已解决`, which writes `metadata.user.quarantine_reviewed=true`, and `物理删除`, which removes the document and is verified by backend `404` plus the row disappearing from the filtered list. Cleanup purges the disposable dataset documents and deletes both datasets (`204`). |
| Browser UI governance workbench workflow (remote web page host) | `artifacts/ui-clickthrough/governance-workbench-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated governance-workbench lane now passes `2/2` in `30.264s`. It still proves the live `智能清洗` workbench path on a real dataset-scoped document, and now also proves the cross-page handoff into `/chunk-preview`: after `保存` and `推送到切块预览`, the deployed frontend preserves `dataset_id` in the route, lands on `/chunk-preview?dataset_id=...`, and shows the same document inside the chunk-preview sidebar with real backend preview stats visible. Cleanup purged the disposable dataset documents and deleted both datasets (`204`). |
| Browser UI chunk preview workflow (remote web page host) | `artifacts/ui-clickthrough/chunk-preview-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated chunk-preview lane now passes `2/2` in `19.823s`. It creates a real dataset plus a real completed Markdown document through the live API, opens `/chunk-preview?dataset_id=...` on the deployed page host, verifies the real backend auto-preview path via visible chunk statistics, and then proves a second real action: `确认入库`, after which the UI flips to `已完成` and backend document count for the dataset increases from `1` to at least `2`. Cleanup purges the disposable dataset documents and deletes the dataset (`204`). |
| Browser UI knowledge workbench workflow (remote web page host) | `artifacts/ui-clickthrough/knowledge-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | A dedicated knowledge-workbench lane passed `1/1` in `9.014s`. The test created a real dataset plus a real completed Markdown document through the live API, opened `/knowledge?dataset=...` on the deployed page host, verified the dataset-scoped document appeared in the list, switched to `检索测试`, ran a real index audit, and verified retrieval-panel signals including `索引审计`, `正常运行`, `向量总数`, `文档总数`, and `分片总数`. Cleanup purged the disposable dataset documents and deleted the dataset (`204`). |
| Browser UI graph workflow (remote web page host) | `artifacts/ui-clickthrough/graph-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The graph lane now passes `1/1` in `25.440s` after the dataset-scope fix in `main@2263880e`. The test creates a real dataset plus two completed Markdown documents through the live API, runs heuristic KG extraction for both, opens `/graph?dataset_id=...` on the deployed page host, and verifies the visible `E:N:L` header matches the exact scoped `/api/v1/kg/stats` response for that dataset before checking `语义索引`, the `节点` section, and a real semantic node button (`聚焦节点：...`). This closes the earlier regression where the page could silently degrade to tenant-global KG counts while the API still returned the correct scoped totals. Cleanup purges the disposable dataset documents and deletes the dataset (`204`). |
| Browser UI graph snapshots workflow (remote web page host) | `artifacts/ui-clickthrough/graph-snapshots-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated graph-snapshots lane now passes `1/1` in `34.775s`. The test creates one real dataset plus two completed Markdown documents with different chunk strategies, verifies they resolve to distinct `active_pipeline_hash` values, runs heuristic KG extraction for both, confirms `/api/v1/kg/snapshots/compare` returns a non-empty diff for those two dataset-scoped hashes, then opens `/graph/snapshots?dataset_id=...` on the deployed page host, fills `手动填写快照 A/B 哈希`, runs `后端对比`, and verifies the UI exits the empty state and renders diff signals including `节点变化`, `属性变化`, `新增关系`, and `删除关系`. Cleanup purges the disposable dataset documents and deletes the dataset (`204`). |
| Browser UI graph diagnostics workflow (remote web page host) | `artifacts/ui-clickthrough/graph-diagnostics-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated graph-diagnostics lane now passes `2/2` in `48.726s`. The first test still proves a real diagnostics run end-to-end: dataset + document + chunk + regression case -> `开始评测` -> raw result payload -> persisted history row. The second test now proves the `抽取数据` tab on the same deployed page host: after creating a real dataset/document and running heuristic KG extraction, the page can `拉取质量报告`, render the `聚合指标` card group, and expose a raw JSON payload containing `summary`, `events`, and `scope`. This second half depends on the PostgreSQL JSON comparison fix in `kg_denoiser.py`, which removed the broken `json = json` relation-noise predicate from the backend quality-report path. Cleanup deletes the regression case and purges/deletes the disposable dataset(s). |
| Browser UI diagnostics workflow (remote web page host) | `artifacts/ui-clickthrough/diagnostics-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated diagnostics lane now passes `1/1` in `23.663s`. The test creates a real dataset plus a real completed Markdown document, preflights both `/api/v1/rag/prompt-preview` and `/api/v1/observability/embedding-drift/snapshot` against that dataset, opens `/diagnostics` on the deployed page host, selects the same dataset, runs `运行 RAG 预览`, expands the page's merged raw-response panel to verify `dataset_id`, `rag_preview`, and the unique diagnostics anchor all appear, then runs `漂移检查` and verifies the same raw-response panel switches from `\"embedding_drift\": null` to a populated object. Cleanup purges the dataset documents and deletes the dataset (`204`). |
| Browser UI governance profiles workflow (remote web page host) | `artifacts/ui-clickthrough/governance-profiles-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated governance-profiles lane now passes `2/2` in `9.946s`. The first test still proves real custom profile create/delete through the deployed `/data-governance/profiles` page. The second test creates a disposable custom profile through the live API, opens the same profile in the real drawer UI, switches to `沙盒测试`, pastes a TOC-heavy sample, clicks `运行`, and verifies the deployed page renders both the cleaned Markdown result and a unified diff that removes `1. 目录 ...... 12`. Cleanup deletes the disposable profile through the live API. |
| Browser UI common-lines workflow (remote web page host) | `artifacts/ui-clickthrough/common-lines-workbench-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated common-lines lane now passes `1/1` in `52.760s`. The test creates a disposable dataset plus three completed Markdown documents that share one unique header line, creates a disposable custom governance profile, preflights `/api/v1/pipeline/learn-common-lines` to confirm the candidate exists, then opens `/data-governance/common-lines` on the deployed page host, selects the same dataset and profile, runs `扫描`, verifies the candidate row and `扫描 3/3 文档` summary, selects that row, clicks `写入配置`, and finally confirms through the live API that the governance profile now contains the expected regex rule. Cleanup deletes the disposable profile, purges the dataset documents, and deletes the dataset (`204`). |
| Browser UI industry rules workflow (remote web page host) | `artifacts/ui-clickthrough/industry-rules-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated industry-rules lane now passes `2/2` in `21.969s`. The first test still proves live ruleset load, rewrite preview, and current-ruleset export for `industrial_control`. The second test temporarily enables metrics logging, creates a disposable dataset plus one completed Markdown document, sends a real chat query containing `PLC9`, waits until the dataset-scoped `rule-suggestions` API returns `PLC9` as a glossary candidate, then opens `/governance/industry-rules`, selects the same ruleset and dataset, clicks the candidate-panel `刷新`, and verifies the deployed page renders both the `PLC9` candidate and the selected dataset badge. Cleanup deletes the disposable conversation, purges and deletes the dataset, and restores the original observability settings. |
| Browser UI observability workflow (remote web page host) | `artifacts/ui-clickthrough/observability-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated observability lane now passes `2/2` in `21.816s`. The first test still proves the page's real ops-panel actions (`依赖健康`, `任务队列`, `SLO`, `Trace 上报`) by expanding the shared raw-response panel and checking live backend payload markers (`mimirq.observability.deps.v1`, `mimirq.task_queue_observability.v1`, `mimirq.slo_snapshot.v1`, and `{ reported: true, event: manual_observability_probe }`). The second test temporarily enables metrics logging, seeds one grounded extractive chat plus one keyword zero-hit extractive chat, then verifies the deployed `/observability` page itself reflects the live counters on both tabs: summary `RAG Trace`, query-analytics `Requests`, `Unique query_hash`, and the rendered shortened zero-hit `query_hash`. Cleanup deletes both disposable conversations, purges the disposable dataset, deletes the dataset, and restores the original observability settings. |
| Browser UI feedback workbench workflow (remote web page host) | `artifacts/ui-clickthrough/feedback-workbench-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated feedback-workbench lane now passes `1/1` in `26.001s`. The test creates a real dataset plus a real completed Markdown document, sends a real extractive chat to generate an assistant message, submits real negative feedback for that assistant message, opens `/knowledge/feedback` on the deployed page host, verifies the feedback detail drawer content, archives the item from the UI, and confirms the backend feedback row flips to `extra.archived=true`. Cleanup deletes the disposable conversation, purges the dataset documents, and deletes the dataset. |
| Browser UI evidence workbench workflow (remote web page host) | `artifacts/ui-clickthrough/evidence-workbench-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated evidence-workbench lane now passes `1/1` in `18.300s`. The test creates a real dataset plus a real completed Markdown document, opens `/knowledge/evidence` on the deployed page host, scopes the query to that dataset, runs a real retrieve-only evidence lookup until `has_evidence=true`, verifies the citations panel renders the retrieved document excerpt, then switches to the evidence operations panel, selects the same dataset, runs `数据集 Drift`, and verifies the drift audit result plus raw payload include the live dataset id. Cleanup purges the dataset documents and deletes the dataset. |
| Browser UI similarity workbench workflow (remote web page host) | `artifacts/ui-clickthrough/similarity-workbench-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated similarity-workbench lane now passes `1/1` in `15.950s`. The test creates a disposable dataset plus one completed Markdown document, waits until `ragviz/similarity/collections` exposes the corresponding `dataset_chunks` collection, preflights `ragviz/similarity/calculate` on that collection against itself, opens `/knowledge/similarity` on the deployed page host, selects the same collection on both axes, runs `计算相似度`, and verifies the page renders the real result with `平均相似度`, `最高相似度`, `高相似占比`, plus the expected collection label in the heatmap header. Cleanup purges the disposable document and deletes the dataset (`204`). |
| Browser UI datasets workflow (remote web page host) | `artifacts/ui-clickthrough/datasets-workbench-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated datasets-workbench lane now passes `1/1` in `16.477s`. The test opens `/datasets` on the deployed page host, creates one real dataset through the UI, confirms through the live API that the dataset record exists, filters the list to that unique dataset, edits the description from the inspector path, confirms the updated description through the live API, and finally deletes the dataset from the UI while verifying the record disappears from the live dataset list. Cleanup only needs an API delete fallback if the UI delete fails. |
| Browser UI parsing workflow (remote web page host) | `artifacts/ui-clickthrough/parsing-workbench-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated parsing-workbench lane now passes `1/1` in `20.117s`. The test creates a disposable dataset plus one real completed Markdown document, opens `/parsing` on the deployed page host, switches the dataset-scope selector to that dataset, waits for the backend-synced library file to appear, clicks it, and then verifies the page renders the selected-file summary, an `已解析` status chip, the persisted markdown content containing the unique parsing anchor, and the library-file inspector sections (`解析详情`, `快捷操作`). Cleanup purges the disposable document and deletes the dataset (`204`). |
| Browser UI dataset precheck workflow (remote web page host) | `artifacts/ui-clickthrough/dataset-precheck-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated dataset-precheck lane now passes `1/1` in `2.966s`. The test creates a disposable dataset, uploads one Markdown file through `upload-batch` with `precheck_only=true`, waits for the resulting precheck scan run to reach `completed`, opens `/datasets/{id}/precheck` on the deployed page host, verifies the selected dataset and summary surface render, then clicks the representative-samples `加载` button and confirms both the `预检代表性样本` table and the real filename/`short_text` bucket appear. Cleanup deletes the disposable dataset (`204`). |
| Browser UI dataset profile workflow (remote web page host) | `artifacts/ui-clickthrough/dataset-profile-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated dataset-profile lane now passes `1/1` in `16.673s`. The test creates a disposable dataset plus one real completed Markdown document, opens `/datasets/{id}/profile` on the deployed page host, verifies the live summary surfaces (`文档总数`, `Chunk 数分布（每文档）`, `Parsing provenance / 路由`), then triggers `导出 JSON` and parses the downloaded payload to confirm it contains a real profile summary with `total_documents>=1` and `by_file_type.md>=1`. Cleanup purges the disposable document and deletes the dataset (`204`). |
| Browser UI dataset health workflow (remote web page host) | `artifacts/ui-clickthrough/dataset-health-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated dataset-health lane now passes `1/1` in `16.594s`. The test creates a disposable dataset plus one real completed Markdown document, opens `/datasets/{id}/health` on the deployed page host, verifies the live summary surfaces (`文档总数`, `入库状态分布`, `格式分布`, `建议（rule-based）`), then triggers `导出 JSON` and parses the downloaded payload to confirm it contains the real `mimirq.dataset_health.v1` schema, the current dataset id, and a health profile with `total_documents>=1`. Cleanup purges the disposable document and deletes the dataset (`204`). |
| Browser UI ingestion monitor workflow (remote web page host) | `artifacts/ui-clickthrough/ingestion-monitor-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated ingestion-monitor lane now passes `1/1` in `17.465s`. The test creates a disposable dataset plus one completed Markdown document, opens `/knowledge/ingestion?mode=execution-monitor&datasetId=...` on the deployed page host, and verifies the execution-monitor path renders `执行监控`, `解析成功率`, `任务列表`, the uploaded filename, and an `已完成` status cell inside the real task table. Cleanup purges the disposable document and deletes the dataset (`204`). |
| Browser UI ingestion operations workflow (remote web page host) | `artifacts/ui-clickthrough/ingestion-operations-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated ingestion-operations lane now passes `1/1` in `17.277s`. The test creates a disposable dataset plus one completed Markdown document, opens `/knowledge/ingestion?datasetId=...` on the deployed page host in operation mode, and verifies the page renders `入库中心`, `当前目标数据集`, the bound dataset name inside the dataset combobox, a `1 文档` dataset summary signal, and the main upload dropzone prompt (`点击选择文件，或将文件拖拽到此处`). Cleanup purges the disposable document and deletes the dataset (`204`). |
| Browser UI regression workbench workflow (remote web page host) | `artifacts/ui-clickthrough/regression-workbench-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated regression-workbench lane now passes `1/1` in `17.838s`. The test creates a disposable dataset plus one completed Markdown document, creates one real regression case with a `golden` tag and one real reference source, opens `/evaluations?tab=regression` on the deployed page host, selects the dataset, verifies the question appears in `Golden 评测集`, clicks `运行 Golden`, and then uses the live API to confirm the dataset-scoped regression run count increases before asserting the new `运行 {id}` row becomes visible in the page's run timeline. Cleanup deletes the disposable regression case, purges the document, and deletes the dataset (`204`). |
| Browser UI retrieval ablations workflow (remote web page host) | `artifacts/ui-clickthrough/retrieval-ablations-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated retrieval-ablations lane now passes `1/1` in `19.923s`. The test creates a disposable dataset plus one completed Markdown document, creates one real `golden` regression case, then creates two completed retrieval-only regression runs against that dataset (`keyword` base, `hybrid` target). On the deployed `/evaluations/ablations` page, it selects the dataset, clicks `生成对比`, switches to `原始数据`, and verifies the raw diff payload contains the real `base_run_id`, `target_run_id`, and `metric_diffs` structure. Cleanup deletes the disposable regression case, purges the document, and deletes the dataset (`204`). |
| Browser UI conversation evaluation workflow (remote web page host) | `artifacts/ui-clickthrough/conversation-evaluation-live-20260524-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated conversation-evaluation lane now passes `1/1` in `17.149s`. The test creates a disposable dataset plus one completed Markdown document, sends one real extractive chat query to create a live conversation, opens `/evaluations?conversation_id=...` on the deployed page host, clicks `开始评测`, and then uses the live API to confirm the conversation-scoped RAGAS run count increases before asserting the page renders the new run card for that unique question with a live status badge. Cleanup deletes the disposable conversation, purges the document, and deletes the dataset (`204`). |
| Graph scope audit (dataset vs explicit document_ids) | `artifacts/graph-scope-audit/remote-20260523/report.json` from `scripts/remote_graph_scope_audit.py` on `10.168.2.253` | pass | A dedicated graph-scope audit created one disposable dataset plus two Markdown documents, ran heuristic KG extraction for both, and then compared `dataset_id` scope against explicit repeated `document_ids` scope on both `/api/v1/kg/stats` and `/api/v1/kg/graph`. The counts matched exactly in both paths (`events=2`, `entities=5`, `stats.links=5`, graph `nodes=7`, graph `links=9`), while the same tenant's unscoped `/kg/stats` stayed at the much larger global totals (`4544 / 4925 / 61580`). Cleanup purged both documents and deleted the dataset (`204`). |
| Browser UI reports workflow (remote web page host) | `artifacts/ui-clickthrough/reports-live-20260523-remote-web.json` from local Playwright + SSH tunnel to the remote `web` and `api` containers on `10.168.2.253` | pass | The dedicated reports lane now passes `2/2` in `20.013s`. It still proves real dataset report loading on the deployed page host, and now also proves a second real action: `导出 JSON`, where the browser download is triggered from the real backend payload and the suggested filename matches the selected dataset report name. Cleanup purges the disposable dataset documents and deletes the dataset (`204`). |
| Knowledge-base boundary matrix | `artifacts/kb-boundary-matrix/remote-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A dedicated KB-boundary proof created two disposable datasets (`alpha`, `beta`) with mutually exclusive tokens, verified dataset-scoped inventory export returned only the owning document, verified dataset-scoped retrieval/chat never leaked the other dataset's token, and verified explicit cross-dataset `document_ids` scope could surface the beta document while staying inside the allowed two-document scope. Cleanup purged and deleted both disposable datasets, and a follow-up sweep removed the failed-run leftovers from earlier iterations. |
| Knowledge-base file-type breadth matrix | `artifacts/kb-format-matrix/remote-20260523/report.json`, `artifacts/kb-format-matrix/remote-broader-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | KB file-type breadth now has two complementary proofs. The original run covered `md`, `html`, `csv`, `json`, `docx`, and `xlsx`. The broadened run added `yaml` and `xml`, for `8` total file families in one disposable dataset. In that broader run, all `8` files completed with non-empty parsed content and at least one chunk, and dataset-scoped retrieve + extractive chat both returned `200` with the expected document present in citations for every format. Cleanup purged all `8` documents and deleted the dataset (`204`). |
| Knowledge-base targeted family extensions | `artifacts/kb-format-matrix/remote-text-families-20260524/report.json`, `artifacts/kb-format-matrix/remote-rare-text-20260524/report.json`, `artifacts/kb-format-matrix/remote-rare-code-20260524/report.json`, `artifacts/kb-format-matrix/remote-pptx-20260524/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | KB file-type breadth now has four targeted extension proofs on top of the earlier 8-family matrix. The first targeted rerun used explicit `--case-name` selectors to extend coverage across `txt`, `ini`, and `sql`; all `3` completed with resolved text parsing, exactly `1` chunk each, non-empty parsed content (`69 / 57 / 72` chars), and dataset-scoped retrieve + extractive chat both returned `200` with self-citations only. The second targeted rerun extended coverage across `toml`, `properties`, `env`, `rst`, and `log`; all `5` completed with exactly `1` chunk each, non-empty parsed content (`58 / 50 / 47 / 77 / 67` chars), and dataset-scoped retrieve + extractive chat both returned `200` with self-citations only. The third targeted rerun extended coverage across `jsonl`, `proto`, `graphql`, `terraform`, and `patch`; all `5` completed with exactly `1` chunk each, non-empty parsed content (`62 / 74 / 64 / 55 / 51` chars), and dataset-scoped retrieve + extractive chat both returned `200` with self-citations only. The fourth targeted rerun proved one office-like binary family, `pptx`; the generated deck completed with `1` chunk, `103` parsed chars, dataset-scoped retrieve `200`, extractive chat `200`, and self-citations only before cleanup purged the disposable document and deleted the dataset (`204`). These targeted proofs complement the earlier `8`-family matrix; an exploratory mixed-corpus rerun also exposed a separate `xlsx` retrieval drift that was not promoted as a passing artifact. |
| Knowledge-base misc text-like extensions | `artifacts/kb-format-matrix/remote-rare-misc-20260524/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A fifth targeted KB extension now covers `ndjson`, `hcl`, `adoc`, `diff`, and `atom`. All `5` documents completed with exactly `1` chunk each and non-empty parsed content (`61 / 58 / 81 / 35 / 129` chars). Dataset-scoped retrieve + extractive chat returned `200` on every case; the expected document appeared in citations for all five cases, and four of the five (`ndjson`, `hcl`, `adoc`, `diff`) returned self-citations only. Cleanup purged the `5` disposable documents and deleted the dataset (`204`). |
| Chunking matrix on real parsed outputs | `artifacts/chunking-matrix/remote-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A dedicated chunking proof created one disposable dataset, ingested `md`, `html`, `csv`, `docx`, `xlsx`, one small PDF, and one RFC-scale long PDF, then verified both persisted metadata and live preview strategies. All `7` documents completed with non-empty parsed content and zero empty persisted chunks. The run proved persisted `chunking_stats` and `chunk_coverage` are now present in `DocumentDetail.metadata`, and dataset profile summary returned non-empty chunk-count / avg-chars / chunk-length / chunk-coverage / overlap-waste histograms across all file types. It also ran `26` live preview checks; the long-PDF spread was explicit: `langchain_recursive=457`, `markdown_hierarchy=4423`, `semantic_sentence=543`, `parent_child=1593` chunks, with coverage staying `0.985-1.000`. Cleanup purged `7` documents and deleted the dataset (`204`). |
| Knowledge-base permission boundary matrix | `artifacts/kb-permission-boundary/remote-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A permission-sensitive KB proof normalized `outsider` to `viewer` and covered three read-scope layers in one run. First, shared-vs-private dataset behavior: shared inventory/retrieve/chat returned `200`, private inventory/retrieve/chat returned `403`, and mixed `document_ids` scope kept only the readable shared document. Second, group-based dataset sharing: a disposable tenant group was created, `outsider` was added, one dataset was shared via `partial_group_list`, and outsider inventory/retrieve/chat returned `200` on that dataset while mixed scope still filtered out the unreadable private document. Third, document-level ACL overrides inside a readable dataset: one visible document, one owner-only doc (`partial_member_list=[demo]`), and one group-only doc (`partial_group_list=[group_id]`) were ingested into the same readable dataset; outsider inventory showed only the visible + group-guarded docs, private-doc direct scope returned `403`, and retrieve/chat never leaked the owner-only token. Cleanup purged/deleted all disposable datasets and deleted the temporary group (`204`), and a follow-up sweep removed earlier failed-run leftovers. |
| Parser concurrency probe (magicpdf) | `artifacts/parser-concurrency/remote-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A controlled `parse-preview` concurrency probe on 4 stable small PDFs (`cross_page_table`, `header_footer_noise`, `merged_header_table`, `mixed_layout`) ran `magicpdf` at concurrency `1/2/4`. All `12` requests returned `200` with `backend=magicpdf` and output above the markdown threshold. Aggregate results: concurrency `1` -> wall `70.773s`, throughput `0.057 rps`, p95 `18.419s`; concurrency `2` -> wall `64.868s`, throughput `0.062 rps`, p95 `33.027s`; concurrency `4` -> wall `64.124s`, throughput `0.062 rps`, p95 `61.638s`. This is the first explicit server-side batch/concurrency evidence for the current standalone MagicPDF path. |
| Parser concurrency probe (magicpdf, large PDFs) | `artifacts/parser-concurrency/remote-large-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A second concurrency probe targeted two RFC-scale PDFs (`rfc9000-quic.pdf`, `rfc9110-http-semantics.pdf`) with the same standalone `magicpdf` service. Both requests succeeded at concurrency `1` and `2` with resolved backend `magicpdf` and large markdown outputs (`355,649` and `442,373` chars). Aggregate results: concurrency `1` -> wall `250.554s`, throughput `0.008 rps`, p95 `144.491s`; concurrency `2` -> wall `251.003s`, throughput `0.008 rps`, p95 `243.787s`. API health remained `healthy` throughout, but the second large request effectively dominates wall time, so this is evidence of stability under large concurrent load, not evidence of throughput scaling. |
| Parser mixed-backend contention probe | `artifacts/parser-contention/remote-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A bounded mixed-backend contention probe ran `magicpdf` and `marker` together on the same 2-page fixture for `2` rounds (`4` total requests). All requests returned `200` with the requested backend and sufficient markdown. Overall wall time was `56.647s` at `0.071 rps`. Per-backend latencies diverged materially: `magicpdf` p50/p95 = `36.489s / 42.614s`, while `marker` p50/p95 = `13.514s / 13.660s`. This is the first explicit server-side evidence for cross-backend contention on the current parser stack. |
| Parser mixed-backend contention probe (`magicpdf + olmocr`) | `artifacts/parser-contention/remote-olmocr-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | partial | A bounded `magicpdf + olmocr` contention attempt on the same 2-page fixture exposed an important limit rather than a clean pass. `magicpdf` still succeeded in `19.183s`, but `olmocr` took `498.099s` and ultimately resolved `backend=basic`, so the run failed the requested-backend check. This means `olmocr` is currently not a viable peer lane for the bounded small-doc contention matrix, even though the API stayed healthy throughout. |
| Parser mixed-backend contention probe (`magicpdf + mineru`) | `artifacts/parser-contention/remote-mineru-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A second bounded mixed-backend contention probe ran `magicpdf` and `mineru` together on the same 2-page fixture for `2` rounds (`4` total requests). All requests returned `200` with the requested backend and sufficient markdown. Overall wall time was `33.496s` at `0.119 rps`. Per-backend latencies were lower than the `magicpdf + marker` lane: `magicpdf` p50/p95 = `25.655s / 32.649s`, while `mineru` p50/p95 = `13.228s / 14.314s`. This makes `magicpdf + mineru` the current strongest passing mixed-backend contention baseline on the server. |
| Parser mixed-backend contention probe (`magicpdf + deepdoc`) | `artifacts/parser-contention/remote-deepdoc-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A third bounded mixed-backend contention probe ran `magicpdf` and `deepdoc` together on the same 2-page fixture for `2` rounds (`4` total requests). All requests returned `200` with the requested backend and sufficient markdown. Overall wall time was `35.753s` at `0.112 rps`, which places it close to the `magicpdf + mineru` lane and materially ahead of `magicpdf + marker`. Per-backend latencies were `magicpdf` p50/p95 = `27.325s / 34.848s` and `deepdoc` p50/p95 = `21.029s / 21.033s`. |
| Parser mixed-backend contention probe (`deepdoc + mineru`) | `artifacts/parser-contention/remote-deepdoc-mineru-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A fourth bounded mixed-backend contention probe moved beyond MagicPDF-dominant pairings and ran `deepdoc` and `mineru` together on the same 2-page fixture for `2` rounds (`4` total requests). All requests returned `200` with the requested backend and sufficient markdown. Overall wall time was `17.592s` at `0.227 rps`, making this the current fastest passing contention lane. Per-backend latencies were `deepdoc` p50/p95 = `17.118s / 17.480s` and `mineru` p50/p95 = `14.695s / 16.151s`. |
| Parser mixed-backend contention probe (`marker + deepdoc`) | `artifacts/parser-contention/remote-marker-deepdoc-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | A fifth bounded mixed-backend contention probe ran `marker` and `deepdoc` together on the same 2-page fixture for `2` rounds (`4` total requests). All requests returned `200` with the requested backend and sufficient markdown. Overall wall time was `32.395s` at `0.123 rps`. Per-backend latencies were `marker` p50/p95 = `23.438s / 31.432s` and `deepdoc` p50/p95 = `29.610s / 30.454s`. This gives one more non-MagicPDF contention baseline and shows marker can stay bounded without MagicPDF in the lane. |
| Parser mixed-size 3-lane contention probe | `artifacts/parser-contention/remote-mixedsize-20260523/report.json` from local script + SSH tunnel to `10.168.2.253` | pass | The contention probe now supports per-backend fixture overrides, which enabled a first mixed-size live run: `magicpdf` parsed an RFC-scale PDF while `deepdoc` and `mineru` simultaneously processed the default 2-page fixture. All `3` requests returned `200` with the requested backend preserved. Overall wall time was `116.604s` at `0.026 rps`; backend latencies were `magicpdf=116.528s`, `deepdoc=14.856s`, `mineru=12.822s`. This is the first explicit server proof that mixed long+short parser load stays bounded. |

## Next Remote Tests

| Priority | Area | Why |
| --- | --- | --- |
| P0 | Product quality backlog | Track the next pass under `plans/server-integration-verification-2026-05-22.md`: parsing serviceization, KG density, chunking on real parsed outputs, prompt quality, knowledge-base lifecycle, governance, and admin/permission boundaries. |
| P2 | Broader parser contention coverage | MagicPDF now has small-PDF and RFC-scale concurrency proofs, five passing mixed-backend contention proofs (`magicpdf + marker`, `magicpdf + mineru`, `magicpdf + deepdoc`, `deepdoc + mineru`, `marker + deepdoc`), one passing mixed-size 3-lane proof, and one failed-but-informative `magicpdf + olmocr` attempt. The next parser pass should target slower backend combinations or heavier 3-lane load rather than rerunning the same pairings. |
| P1 | Broader UI workflow breadth | Deployment wiring and one deeper upload/parse/chat workflow are now proven on the deployed remote `web` page host. The next UI pass should expand breadth across more product journeys, not re-prove the same single-document smoke. |

## Known Gaps

- Broader product verification is now tracked in `plans/server-integration-verification-2026-05-22.md`, covering parsing, KG, chunking, prompts, knowledge-base lifecycle, governance, and admin/permission behavior. Remote test artifacts should link back to that task instead of becoming disconnected one-off runs.
- Governance no longer relies only on `/pipeline/clean-preview` smoke: there is now one live ingest proof covering content sanitization, retrieval-safe citations, and quarantine behavior on the server. Remaining governance work should focus on breadth (more profiles/file types), not re-proving the same five cases.
- Governance now also has deployed-frontend UI proofs for both the quarantine review path and the main governance workbench cleaning path, including the handoff from governance into chunk-preview. Remaining governance work should focus on broader replay/tuning flows or additional profile/file-type breadth, not on re-proving these basic UI loops.
- Prompt workflows no longer rely only on local builtin-library tests. There is now one live server proof covering answer prompt selection, KG extract prompt selection, document testset generation, and regression LLM judge prompt selection. Remaining prompt work should focus on breadth and prompt quality, not baseline wiring.
- KG usefulness now has both a strict targeted multi-hop proof and a broader `5`-question synthetic proof that includes summary-ish prompts. Remaining KG work should shift from “does graph help at all?” to broader real-world question coverage and product presentation, especially on long-document generic summaries.
- Knowledge Base now has dedicated server proofs for mixed file-type corpus breadth across `md`, `html`, `csv`, `json`, `yaml`, `xml`, `docx`, and `xlsx`, plus owner-scoped dataset boundaries and outsider/viewer permission-sensitive filtering across dataset scope, group-based dataset sharing, and document-level ACL overrides. Remaining KB work should move to rarer binary/document families and more complex multi-group/document-sharing combinations instead of re-proving the same baseline trust boundaries.
- Chunking on real parsed outputs no longer relies on one-off PDF spot checks. There is now a dedicated server proof covering persisted chunk metadata plus live preview strategy spread across `md`, `html`, `csv`, `docx`, `xlsx`, small PDF, and RFC-scale long PDF. Remaining chunking work should focus on less common strategies / file families and product default selection, not on re-proving baseline persisted stats or coverage wiring.
- Browser UI now has a broad set of passing Playwright live proofs across business-surface smoke, live-stack upload/parse/chat, management mutations, usage quotas, quarantine review/delete, governance workbench handoff, chunk-preview confirm-ingest, knowledge retrieval audit, graph rendering and snapshot compare, graph diagnostics, diagnostics preview/drift, governance profiles sandboxing, governance common-lines rule generation, industry rules rewrite/candidate refresh, observability ops + metrics, reports export, feedback triage, evidence lookup/drift, similarity calculation, ingestion monitor/operations, regression workbench, retrieval ablations, and conversation evaluation. Remaining UI work should broaden journey coverage rather than re-prove these baseline lanes.
- The dedicated `remote-web` Playwright lane now passes (`web/playwright.remote-web.config.ts` + `pnpm e2e:live:remote-web`) after the history hydration guards and remote `web` container rebuild from `main@059028d9`. Keep this lane for future deployment regression checks.
- Cross-encoder cold start is no longer allowed to stall the only API process while HuggingFace weights download. `main@ca40dd49` bounds local reranker load waits via `RERANKER_LOCAL_LOAD_TIMEOUT_SEC=2.0`, letting live chat degrade to base retrieval order while the background load continues.
- `scripts/production_readiness_chain.py` is now proven on both the remote host path and the current API-container path. The remaining caveat is operational rather than functional: inside `docker-mimirq-api-1`, the default `python` is still not the app venv, so manual invocations must keep using `/opt/venv/bin/python`. `scripts/remote_api_chain_smoke.py` still remains the lighter dependency-free option when only a fast smoke is needed.
- Explicit extractive chat responses are no longer invisible to observability dashboards. `main@0f4aedb5` now emits a minimal `rag_trace` during finalize for `generation_fallback_kind=extractive_retrieval_summary`, and the new `remote_observability_metrics_probe.py` proof shows both metrics summary and query analytics ingest those events (`rag_trace_count` and `zero_hit_count` both advance after a bounded two-chat probe). The remaining observability gap is the deployed page-host metrics-summary / query-analytics lane, not the backend logging path itself.
- KG scale is functionally passing for 12 synthetic documents. Next optimization is extraction density/latency tuning on larger real PDFs, not another small-doc smoke rerun.
- KG event density and usefulness need product discussion before changing defaults. The current 12-doc scale guard generated 12 events because each synthetic document contains one obvious incident; this proves the channel is bounded and stable, but not that the event budget is useful for real KG-assisted RAG. Later discussion should compare baseline RAG vs KG-assisted RAG on real questions before increasing extraction density.
- Qianfan OCR is passing with the online Baidu Qianfan `deepseek-ocr` path on the remote server. Keep `QIANFAN_OCR_PROMPT` empty for that endpoint unless retested; the wrapper selects a short OCR prompt because longer Markdown conversion prompts returned empty output in live probes.
- OLMOCR, Docling, and MagicPDF are functional but slow on the small 2-page smoke PDF. On the 144-page PDF, HTTP 200 alone is not enough: direct serial runs showed `olmocr` can complete in about 495s on a clean GPU, `paddle_vl` still fails after about 49 minutes, and `marker` still times out after about 7100s even when the earlier service-readiness bug is removed. `scripts/remote_pdf_parser_performance.py` and `scripts/remote_parser_service_matrix.py` now treat requested/resolved backend mismatch as a failure class.
- Follow-up on real parsed outputs: the same 144-page MagicPDF document (`f20f6207-f808-4bd9-a8ae-8c2d92455793`) persisted `670` chunks and about `816k` chars after ingestion, but chunk-preview strategy spread is very large on the raw PDF path (`langchain_recursive=1151`, `parent_child=3702`, `semantic_sentence=1272`, `markdown_hierarchy=6203`). This needs product selection rather than assuming one default fits all long PDFs.
- Follow-up on real KG density: a default LLM KG extraction probe on dataset `cd232ae7-609d-415f-a43a-cca768aee664` was still running after `250+` extraction batches and had not committed final graph stats inside the probe window. This is enough evidence that long-document KG extraction needs a tighter bounded policy before it can be treated as a default production path.
- Parser batch/concurrency is no longer completely unmeasured: there is now one controlled `magicpdf` small-PDF baseline, one RFC-scale large-PDF baseline, five passing mixed-backend contention proofs (`magicpdf + marker`, `magicpdf + mineru`, `magicpdf + deepdoc`, `deepdoc + mineru`, `marker + deepdoc`), one passing mixed-size 3-lane proof, and one failed-but-informative `magicpdf + olmocr` contention attempt. Remaining parser performance work should move to slower backend combinations or heavier 3-lane load instead of re-proving these exact pairings.
- Follow-up on the KG budget fix: after deploying `main@154af84`, the same 144-page PDF was re-run with `KG_EXTRACT_MAX_CHUNKS_PER_DOCUMENT=120` and `uniform` sampling. Server logs showed the extraction restarted at batch `1` and advanced through about batch `98` before moving into embedding/indexing work, instead of exploding past `250+` extraction batches. The bound is now effective, but the default low-cost LLM backend still appears too slow for a comfortable long-document default.
- Follow-up on the long-document backend route: after deploying `main@57f7c75`, the same 144-page PDF automatically switched to the heuristic KG extractor on the default path (no explicit backend override). The synchronous extract completed in `13.411s` with `event_count=120`, and final stats for dataset `cd232ae7-609d-415f-a43a-cca768aee664` reported `120 events / 1148 entities / 1887 links`. This is the first server-side evidence that the long-document KG default is now both bounded and operationally fast enough to be usable.
- Follow-up on graph usefulness: long-document generic summary now has positive evidence on two distinct real corpora. The 151-page RFC proof (`artifacts/real-pdf-chain/rfc9000-summary-20260523/report.json`) shows `119 events / 760 entities / 1815 links`, and all three generic summary questions returned non-zero `kg_search` clues (`90 / 90 / 90`) with `10` events each. The 144-page LLM-survey proof (`artifacts/real-pdf-chain/llm-survey-summary-20260523-manual/report.json`) also now shows non-zero `kg_search` clues (`90 / 90 / 90`) for `What is this paper about?`, `Summarize this paper in one sentence.`, and `What are the main themes discussed in this paper?` after heuristic KG extraction refreshed the document graph (`120 events / 1148 entities / 1887 links`). The remaining graph-usefulness gap is no longer “generic summary on long docs”, but broader long-document answer-quality lift across more corpora and question styles.
- Follow-up on permission realism: this deployment host still allows dev-style tenant bootstrap for previously unseen account ids, so outsider-deny verification has to pin a disposable account back to `viewer` before testing. That means the current permission matrix proves enforcement after role normalization, but not a strict “unknown account fails closed at the membership layer” production posture.
- The API container's default `python` is not the app virtualenv. For direct parser debugging inside `docker-mimirq-api-1`, use `/opt/venv/bin/python`; otherwise imports such as `langchain_core` will appear missing even though they exist in the app venv.
- Direct `magicpdf` inside the API container remains intentionally non-production and still should not be used as the deployment path. The validated production path is the standalone `mimirq-magicpdf` service image, which now also patches MagicPDF's internal `lang.ch` OCR model mapping to the shared `ch_PP-OCRv5_rec_infer.pth` cache layout when the older doc-recognition checkpoint is absent.
