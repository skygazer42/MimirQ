import sqlalchemy
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import JSONB
col = sa.column(" x\, JSONB)
expr = col[\a\]
print(sqlalchemy.__version__)
print([m for m in dir(expr) if m.startswith(\as_\)])
