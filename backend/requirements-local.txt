# Local embedding mode (optional)
#
# Install base deps first:
#   pip install -r requirements.txt
#
# Then install local embedding deps:
#   pip install -r requirements-local.txt
#
# NOTE:
# - PyTorch wheels vary by OS/CUDA. For GPU builds, follow official install guide:
#   https://pytorch.org/get-started/locally/

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.4.1
torchvision==0.19.1
sentence-transformers>=2.2,<4
transformers>=4.30,<5
