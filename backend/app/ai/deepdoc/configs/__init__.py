"""
DeepDoc configuration package.

This backend vendors a minimal DeepDoc checkout. The upstream project
expects its configuration module to live under `deepdoc.configs` (we keep
it co-located with the vendored source tree).
"""
