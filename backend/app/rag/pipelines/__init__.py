"""
RAG pipelines.

This package hosts multiple interchangeable implementations of the end-to-end
RAG flow (retrieve -> build context -> generate).

Note: Some pipelines (e.g. LangGraph) are optional dependencies. Import their
modules directly to avoid importing heavy/optional packages at startup.
"""

__all__: list[str] = []
