"""
API request/response data models.

Defines Pydantic validation models for API interfaces.

Notes:
- Internal service orchestration dataclass/Enum types have been migrated to `app/types/`
- Import `app.api.schemas.<xxx>` (Pydantic) or `app.types.<xxx>` (internal types) directly in business code
"""
