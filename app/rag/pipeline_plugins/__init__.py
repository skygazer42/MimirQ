"""
Platform contracts for registered pipeline plugin packages.

MimirQ validates and invokes plugin package stages, but domain-specific
governance, chunking, and KG rules live in the configured plugin directories.
"""
