"""
RAG components and contracts.

This package defines lightweight interfaces/contracts to support multiple
pipeline implementations (e.g. streaming engine vs graph runner) with shared
schemas and pluggable building blocks.
"""

